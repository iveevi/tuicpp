builds:
  - demo_release:
    - sources: 'demo/main.cpp, demo/plain_window.cpp'
    - libraries: 'ncurses'

targets:
  - demo:
    - builds:
      - default: demo_release
    - postbuilds:
      - default: '{}'
